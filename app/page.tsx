export default function Home() {
  return (
    <main style={{ fontFamily: "system-ui", padding: "2rem" }}>
      <h1>CanvasApp MCP Server</h1>
      <p>POST JSON-RPC requests to <code>/mcp</code> to use MCP tools.</p>
    </main>
  );
}
